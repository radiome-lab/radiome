language: python
python:
    - 3.8
    - 3.7
    - 3.6

# Command to install dependencies, e.g. pip install -r requirements.txt --use-mirrors
install: pip install -U tox-travis codecov git+https://github.com/radiome-lab/preprocessing.git

# Command to run tests, e.g. python setup.py test
script: tox

