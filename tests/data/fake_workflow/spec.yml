radiomeSchemaVersion: 1.0

class: workflow

name: fake workflow

inputs:
    fake_input:
        type: boolean
        doc: a fake inputs.

outputs:
    image_output:
        type: Directory
        doc: fake outputs.
