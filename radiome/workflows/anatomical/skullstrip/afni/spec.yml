radiomeSchemaVersion: 1.0

class: workflow

name: afni-skullstrip

inputs:
    mask_vol:
        type: boolean
    shrink_factor:
        type: float
    var_shrink_fac:
        type: boolean
    shrink_fac_bot_lim:
        type: float
    avoid_vent:
        type: boolean
    niter:
        type: integer
    pushout:
        type: boolean
    touchup:
        type: boolean
    fill_hole:
        type: integer
    smooth_final:
        type: integer
    avoid_eyes:
        type: boolean
    use_edge:
        type: boolean
    exp_frac:
        type: float
    push_to_edge:
        type: boolean
    use_skull:
        type: boolean
    perc_int:
        type: integer
    max_inter_iter:
        type: integer
    fac:
        type: integer
    blur_fwhm:
        type: integer
    monkey:
        type: integer

outputs:
    image_output:
        type: Directory
        doc: Output for the result, conform to BIDS standard.
