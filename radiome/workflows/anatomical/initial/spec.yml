radiomeSchemaVersion: 1.0

class: workflow

name: initial-processing

inputs:
    non_local_means_filtering:
        type: boolean
        doc: enable non local means.
    n4_bias_field_correction:
        type: boolean
        doc: enable n4 bias correction.

outputs:
    image_output:
        type: Directory
        doc: Output for the result, conform to BIDS standard.
